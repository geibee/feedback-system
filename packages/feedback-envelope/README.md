# @geibee/feedback-envelope

RFC 8785相当のcanonical JSON、HS256、用途別domain separator、key ringを実装するcodecです。Envelope、message marker、attachment mappingの署名／検証、command hash、credential鍵と分離したparticipant ID導出を提供します。
